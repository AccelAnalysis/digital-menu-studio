# Conflict Resolution

If a remote save fails because another teammate updated the menu, the editor displays a modal showing who saved last and when. Reload to pull their changes or force save to overwrite.
